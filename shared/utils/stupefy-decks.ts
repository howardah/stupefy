import { buildMainDeck } from "./game-data/build-main-deck";
import { CHARACTER_CATALOG } from "./game-data/character-catalog";

export const mainDeck = buildMainDeck;
export const characters = CHARACTER_CATALOG;
